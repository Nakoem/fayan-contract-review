# 简历 — AI 应用项目工程师

## 个人信息

- **GitHub**：github.com/pppppgy
- **求职方向**：AI 应用项目工程师
- **所在地**：（你自己填）

---

## 技能矩阵

| 类别 | 技术 |
|------|------|
| **AI & LLM** | ReAct Agent、Function Calling、RAG、Prompt 工程、MCP 协议、流式输出、LLM-as-Judge |
| **对话系统** | 多轮对话记忆、流式输出（SSE/Streaming）、跨库RAG检索 |
| **后端 & API** | Python、FastAPI、REST API 设计、异步任务 |
| **向量检索** | Chroma 向量数据库、DashScope Embedding、混合检索（语义 + 关键词） |
| **工程化** | Docker 容器化、docker-compose 编排、loguru 日志系统、YAML 配置管理 |
| **前端** | Streamlit、OCR 图片识别集成 |
| **工具 & 协议** | MCP（Model Context Protocol）、OpenAI 兼容 SDK、Git |

---

## 项目经历

### 法眼 · AI 合同审查 Agent（4个月，独立开发）

> GitHub: github.com/pppppgy/fayan-contract-review
> 一个从零到产品级的 AI 合同审查系统，覆盖 6 种合同类型，内置法规/判例/地方政策/税务四重知识库。

#### 核心贡献

**1. ReAct Agent 架构设计与实现**
- 设计 10 工具 ReAct（Reasoning + Acting）自主决策循环，Agent 自动完成"提取条款 → 查法规 → 逐条分析 → 完整性检查 → 生成报告"全流程
- 支持 6 种合同类型（房屋租赁/劳动/买卖/服务/合作/借款），每种配备法条级法定红线标准

**2. RAG 检索增强生成全链路**
- 将 66 条硬编码法规/判例/政策/税务知识迁移到 Chroma 向量数据库（216 chunks）
- 使用 DashScope Text-Embedding 实现语义检索，解决关键词匹配"押金不退"无法命中"押金退还"类问题
- 设计混合检索策略（向量语义检索 + 关键词精确匹配兜底），保证检索鲁棒性

**3. MCP 协议标准化**
- 将 5 个法律搜索工具封装为 MCP Server（Model Context Protocol，Anthropic 2024 年发布的 AI 工具互操作协议）
- 任何支持 MCP 的 AI（Claude Code、Cursor 等）可直接通过 stdio 调用法眼的法规/判例/政策搜索能力

**4. FastAPI + Docker 容器化部署**
- 将 Streamlit 单体应用升级为 FastAPI REST 服务（同步/异步审查、Swagger 文档、Health Check）
- 编写 Dockerfile（多阶段构建）+ docker-compose.yml，支持一键部署

**5. 工程化基础建设**
- **日志系统**：基于 loguru 实现 CLI/Web 双模式日志，文件轮转（10MB），API 调用延迟追踪
- **Prompt 版本管理**：15 个提示词 YAML 外部化 + 惰性代理模式，YAML 缺失自动回退硬编码
- **自动化评估**：三维度评估框架（格式合规检查 + 风险召回率 + LLM-as-Judge 打分），47 个已知风险测试点

**6. 模型选型与评测**

对 4 个模型进行了完整的 Function Calling + 报告质量对比测试：

| 模型 | JSON合规 | 格式规范 | 法律判断 | 速度 | 结论 |
|------|:--:|:--:|:--:|:--:|------|
| **qwen-plus** | ⚠️ 偶有JSON错误 | ✅ 遵守纯文本模板 | ✅ 判断准确 | ✅ | **最终选择** |
| qwen-max | ✅ JSON稳定 | ✅ 格式合规 | ❌ 法律分析保守降级 | ✅ | 法律判断不够锐利 |
| qwen3.6-plus | ✅ JSON稳定 | ⚠️ 间或偏离模板 | ⚠️ 思考模式偏冗长 | ❌ 思考拖慢3-5倍 | 速度不可接受（审查耗时分+） |
| deepseek-v3.2 | ✅ JSON稳定 | ❌ 顽固输出Markdown | ✅ 判断准确 | ✅ | 改3版提示词仍输出Markdown，法务系统不兼容 |

**选型逻辑**：不只看模型榜单排名，而是围绕具体任务（合同审查）定义 4 维评测标准——JSON合规、报告格式遵守、法律判断质量、响应速度。最终选 qwen-plus 是因为它在这项任务上最"听话"，缺陷可控（JSON 错误已有文本格式兜底方案）。

**7. 疑难技术攻坚**

| 问题 | 排查深度 | 最终方案 | 体现能力 |
|------|---------|---------|---------|
| qwen-plus JSON格式错误（`400 InvalidParameter`） | 8轮迭代：修复重试逻辑→客户端JSON修复→精简提示词→纯文本兜底→文本格式工具调用+会话存储 | `<<TOOL>>`标签绕过DashScope校验 | 问题定位不靠猜，一轮轮实验逼近根因 |
| DeepSeek顽固输出Markdown | 改3版提示词无效→分析是模型RLHF偏好 | 切回qwen-plus（选模型看"听话程度"，不只看榜单） | 不迷信模型参数，以实际任务效果为准 |
| 合同类型5→13→6的扩展与回退 | 扩展8种→JSON错误暴增→根因是提示词越长JSON越差 | 回退到6种高频类型，每种法条红线反而写得更细 | 敢于承认错误方向，果断回退而非死扛 |
| 报告被模型"总结"覆盖 | 工具报告完整→模型下轮自己总结一笔→最终输出是缩水版 | last_report无条件优先返回 | 对LLM输出不可控有深刻认知，加硬约束而非寄望提示词 |
| 模型逐字输出内部铁律 | 铁律放报告模板底部→被当正文输出 | 铁律移到System Prompt，模板只留结构 | 理解LLM的"指令跟随"边界 |

**8. 系统设计决策**
- **为什么用 ReAct Agent 而不是固定流水线？** — 固定流水线"一步出错全崩"，ReAct 让 AI 自主决定调用什么工具、传什么参数，容错和灵活性大幅提升
- **为什么先做工程化再做 RAG？** — 日志、Prompt 版管、评估体系是地基。没有评估就上 RAG，改完不知道是好是坏
- **为什么选 Chroma 而不是 Milvus/Pinecone？** — 67条知识~23K字符，Chroma 嵌入式无需单独服务，部署简单。数据量大了可平滑升级到 Milvus
- **为什么 MCP 协议放最后？** — 先把工具做稳定、接口定型，再暴露为标准协议。前置 MCP 会导致下游调用方频繁适配变更

#### 技术栈

```
Python | FastAPI | Streamlit | Docker | Chroma | MCP SDK
阿里云百炼 DashScope (qwen-plus) | OpenAI 兼容 SDK | loguru | PyYAML
```

#### 面试话题准备

- **展示深度**：qwen-plus JSON 错误的 8 轮排查 → 每一轮试了什么、排除了什么、为什么最终方案有效
- **展示广度**：Agent 设计 → RAG → 对话系统 → MCP → Docker，两个项目覆盖 AI 应用全栈
- **展示判断力**：合同类型 5→13→6 的回退决策 → 不是所有扩展都是正确的，承认方向错误比死扛更难得
- **展示数据意识**：RAG vs 关键词匹配的效果对比（"押金不退"→"押金退还"命中率从0到0.91）

---

### 法眼 · 法律问答 Bot（1周，独立开发）

> 基于法眼四重知识库的 AI 法律对话助手。Streamlit 聊天界面，支持流式对话和双模式切换。

- **跨库 RAG 检索**：单次查询同时检索法规/判例/政策/税务/资讯 5 个向量库，按相似度融合排序
- **流式输出**：基于 SSE 的实时流式回复，逐字显示，支持中途打断和上下文连续
- **双模式切换**：不传合同 → 泛法律问答；上传合同 → 针对合同条款的定向问答
- **对话记忆**：保留最近 20 轮对话上下文，支持多轮追问和话题切换
- **法条引用**：每条回答注明法规出处（如"《民法典》第 585 条"），避免 LLM 幻觉

**技术栈**：`Streamlit` | `SSE 流式` | `RAG 跨库检索` | `多轮对话记忆`

---

## 开源 & 技术博客

- **法眼项目开源**：github.com/pppppgy/fayan-contract-review
- **技术复盘文章**：《从零到一：我用 ReAct Agent 做了一个合同审查工具，踩了 5 个深坑》（待发布）

---

## 自我评价

- **全链路交付能力**：不只会调 API——能从零设计 Agent 架构、搭建 RAG 检索系统、封装 REST API、写 Dockerfile、建评估体系，一个项目打通 AI 应用全栈
- **系统性排错思维**：面对 LLM 的"幻觉"类问题（JSON 格式错、Markdown 输出、铁律污染报告），不靠运气靠实验——假设→验证→排除→收敛，8轮迭代最终定位
- **技术判断力**：不跟风——能根据实际场景选择技术方案（为什么 Chroma 而不是 Milvus、为什么回退到 6 种合同、为什么 MCP 放最后），每个决策有数据或逻辑支撑
- **前沿敏感度**：主动学习并实践 MCP 协议（Anthropic 2024年底发布），将其作为项目差异化亮点
